# AIDataInsight-Android

第一版 Android 骨架，按当前 iOS 重构后的结构镜像搭建。

当前目标：

- 建立 `app / core / feature` 分层
- 固化 `session / repository / snapshot / viewmodel` 这些跨端一致概念
- 为后续 AI 协作生成真实业务代码提供稳定落点

建议先从 `feature/login` 和 `feature/setting` 开始填充业务实现。
